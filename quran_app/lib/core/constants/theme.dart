import 'package:flutter/material.dart';

class ThemeConstants{
  static const Locale englishLocale = Locale("en");
  static const Locale arabicLocale = Locale("en");
  static const String fontFamily = "Tajawal";
}